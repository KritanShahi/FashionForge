// src/components/TransitionImage.js
/*import React from 'react';
import styled from 'styled-components';

const TransitionImageContainer = styled.div`
  position: relative;
  width: 100%;
  height: 300px;
  overflow: hidden;
`;

const TransitionImages = styled.img`
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.3s ease;
  
  &:hover {
    transform: translateX(20px);  // Move image horizontally on hover
  }
`;

const Transition= ({ src, alt }) => {
  return (
    <TransitionImageContainer>
      <TransitionImages src={src} alt={alt} />
    </TransitionImageContainer>
  );
};

export default Transition;
*/
